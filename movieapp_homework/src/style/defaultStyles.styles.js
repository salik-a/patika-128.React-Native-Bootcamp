import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  flexDefault: {
    flex: 1,
  },
  noFlex: {
    flex: 0,
  },
  row: {
    flexDirection: "row",
  },
});

export default styles;
